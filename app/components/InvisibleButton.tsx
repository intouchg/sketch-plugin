import React from 'react'
import styled from 'styled-components'
import { Button } from '@i/components'

const InvisibleButton = styled(Button).attrs({ variant: 'Invisible' })``

export { InvisibleButton }